package Array; // NEED TO WORK 

import java.util.Arrays;

public class RangeSum {
    public static void main(String[] args) {
        int[] nums = { 1, 2, 3, 4 };
        int n = nums.length;
        // int result = rangeSum(nums, n, 1, 5);
    }

    public static void rangeSum(int[] array, int n, int left, int right) {
        int[] array1 = new int[n * (n + 1) / 2];
        int i = 0;
        int j = 0;
        // for( int )
        while (i < 3 || j < 2) {

        }
    }
}
