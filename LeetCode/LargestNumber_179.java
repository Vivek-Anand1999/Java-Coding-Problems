package LeetCode;

public class LargestNumber_179 {
    public static void main(String[] args) {
        int[] array = {2, 10};
//        String name = Arrays.toString(array); // convert to string not string array
//        Arrays.stream(array).mapToObj(String::valueOf).toArray(String[]::new);
//        System.out.println(s.length);
    }
}
/**
 * using java 8
 * ! String[] stringArray = Arrays.stream(array).mapToObj(String::valueOf).toArray(String[]::new);
 * <p>
 * using using loop
 * ! stringArray[i] = String.valueOf(array[i]);  // Or Integer.toString(array[i])
 */